# Python dependencies for edia-racialized systematic review tooling.
#
# NOTE: All scripts in 04_database_search/ currently rely only on the Python
# standard library (urllib, csv, json, xml.etree, difflib, datetime, etc.),
# so no third-party packages are strictly required to reproduce the search
# pipeline. Python 3.11+ is recommended.
#
# This file is kept as a placeholder so contributors can add dependencies
# here as the project grows (e.g. switching urllib to requests, adding
# pandas for analysis, etc.).

# --- Optional / future dependencies (uncomment as needed) ---
# requests>=2.28
# pandas>=2.0
# lxml>=4.9
