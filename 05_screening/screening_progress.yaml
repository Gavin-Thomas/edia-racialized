total_items: 10904
batch_size: 150
batches_completed: 73
estimated_batches: 73
screening_type: scoping_review
decision_framework: MAP/OUT_OF_SCOPE/UNCERTAIN
input_file: "../04_database_search/abstracts/refined_all.csv"
output_file: "screened_results.csv"

# PRISMA 2020 terminology crosswalk:
#   MAP (Mapped to Objectives) = PRISMA "Include"
#   OUT_OF_SCOPE              = PRISMA "Exclude"
#   UNCERTAIN                 = PRISMA "Seek additional information"
# The MAP terminology is used in screening because it emphasises alignment
# with the scoping review objectives (JBI) rather than binary eligibility.
# For PRISMA flow diagram reporting, convert:
#   MAP → "Reports included in review"
#   UNCERTAIN → "Reports awaiting classification" or resolve to MAP/OUT_OF_SCOPE
prisma_crosswalk:
  MAP: "Included"
  OUT_OF_SCOPE: "Excluded"
  UNCERTAIN: "Awaiting classification"
running_totals_pre_qa:
  # These are raw totals BEFORE QA audit corrections.
  # For post-QA final numbers, see SCREENING_COMPLETE.md.
  screened: 10904
  map: 156
  out_of_scope: 10540
  uncertain: 204
  missing: 4  # 4 records not present in batch CSVs (likely batch boundary artifacts)
  conflicts: 0
  agreement_rate: 0.96
  inclusion_rate: 0.014
post_qa_totals:
  confirmed_map: 56
  uncertain_for_fulltext: 38
  out_of_scope: 10810
  forwarded_to_fulltext: 134  # was 129; +5 for propagated QA MAP upgrades (retroactive 2026-04-11)
  reports_not_retrieved: 1    # PRISMA-strict: Record #113 (PMID 41218611, Semaglutide) unobtainable and dropped from included set
  pdfs_pending_manual_retrieval: 2  # Records #132 (PMID 34637343 PRC-063) and #133 (PMID 40135470 MIRAGE) — included but PDFs pending manual retrieval
  included_after_fulltext: 69  # was 65; +5 new QA-upgraded MAPs, -1 dropped PMID 41218611 (Semaglutide — full text unobtainable)
retroactive_correction_2026_04_11:
  # On 2026-04-11, a consolidated audit found that 5 records confirmed MAP by QA
  # (per qa_audit_corrections.md) had been omitted from fulltext_screening_decisions.csv.
  # These were added as records 130-134 in that CSV. Simultaneously, PMID 41218611
  # (Semaglutide for cognitive dysfunction in MDD) was dropped from the included set
  # because the full text is unobtainable (no open-access version identified via OpenAlex or
  # Crossref metadata, not in PubMed Central / Europe PMC, publisher-paywalled).
  added_map_records:
    - pmid: 29338621  # CDP-choline sensory gating schizophrenia (Ottawa) — batch 27
    - pmid: 36325158  # Buprenorphine augmentation TRD (CAMH) — batch 27
    - pmid: 34637343  # PRC-063 methylphenidate adolescent ADHD (UBC) — batch 31
    - pmid: 40135470  # Mirtazapine chronic insomnia MIRAGE (Quebec) — batch 33
    - pmid: 38445382  # Intranasal oxytocin + psychotherapy MDD (Concordia/McGill) — batch 23
  dropped_from_included:
    - pmid: 41218611  # Semaglutide for cognitive dysfunction MDD — full text unobtainable
  net_change:
    forwarded_to_fulltext: +5
    included_after_fulltext: +4  # +5 new, -1 dropped
# SCREENING COMPLETE: 2026-04-01
# Batches 1-22: prior session (125 MAP)
# Batches 23-42: this session, single-screened + QA audited (+20 MAP after corrections)
# Batches 43-47: this session, dual-screened (+4 MAP)
# Batches 48-73: this session, dual-screened + QA audited (+7 MAP after corrections)
# Two QA audits completed: batches 23-42 and batches 48-73
# QA corrections: +11 upgrades, -4 reclassifications across both audits
# Note: QA audit on 2026-04-01 corrected batches 23-42.
# Net MAP unchanged (149) but composition corrected:
#   +6 upgraded UNCERTAIN->MAP, -3 reclassified MAP->OUT_OF_SCOPE, -3 downgraded MAP->UNCERTAIN
# 46 UNCERTAIN->OUT_OF_SCOPE (mostly secondary analyses)
# See qa_audit_corrections.md for details.
